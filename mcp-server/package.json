{
  "name": "arcade-mcp-server",
  "version": "0.2.0",
  "description": "ARCADE MCP Server — TypeScript stub (see server.py for reference implementation)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "note": "TypeScript stub — FastMCP (server.py) is the reference implementation"
}
